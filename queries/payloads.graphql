query getPayloads($protocol: String!) {
  topPayloads(input: { protocol: $protocol }) {
    payloads {
      protocol
      size
      payload
      payload_b64
      request_count
      source_ip_count
      pervasiveness
      source_ips
    }
  }
}
