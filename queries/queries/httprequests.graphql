query TopHTTPRequests {
  topHTTPRequests {
    queryInfo {
      resultsAvailable
      resultsLimit
    }
    httpRequests {
      method
      path
      pervasiveness
      request_count
      request_headers
      source_ip_count
    }
  }
}
