query NoiseRank($ip: String!) {
  noiseRank(ip: $ip) {
    queryInfo {
      resultsAvailable
      resultsLimit
    }
    ips {
      country_pervasiveness
      ip
      noise_score
      payload_diversity
      port_diversity
      request_rate
      sensor_pervasiveness
    }
  }
}
